export type ProductGridItem = {
  id: string | number;
  name: string;
  description: string;
  image: string;
  unit: string | null;
  price: number | null;
  quantity: number | null;
  porijeklo: string | null;
  klasa: string | null;
};
